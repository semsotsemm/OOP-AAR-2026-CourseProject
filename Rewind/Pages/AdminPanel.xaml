<Window x:Class="Rewind.Pages.AdminPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:help="clr-namespace:Rewind.Helpers"
        Title="Rewind — Администратор"
        Icon="{help:Icon rewind_logo.png}"
        Height="780" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#F5F4F0">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Admin Panel" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="Статистика и управление пользователями" FontSize="13" Foreground="#666"/>
            </StackPanel>
            <Button Grid.Column="1" Content="Обновить" Click="Refresh_Click" Padding="14,8"/>
        </Grid>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,16">
            <Border Background="White" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Пользователи" Foreground="#666"/>
                    <TextBlock x:Name="UsersCountText" FontSize="30" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Треки" Foreground="#666"/>
                    <TextBlock x:Name="TracksCountText" FontSize="30" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="Плейлисты" Foreground="#666"/>
                    <TextBlock x:Name="PlaylistsCountText" FontSize="30" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="12">
            <DataGrid x:Name="UsersGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="60"/>
                    <DataGridTextColumn Header="Ник" Binding="{Binding Nickname}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="220"/>
                    <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="140"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120"/>
                    <DataGridTextColumn Header="Треков" Binding="{Binding TracksCount}" Width="90"/>
                    <DataGridTemplateColumn Header="Действия" Width="250">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Бан/Разбан" Click="BanUnban_Click" Margin="0,0,6,0" Padding="8,4"/>
                                    <Button Content="Повысить" Click="Promote_Click" Margin="0,0,6,0" Padding="8,4"/>
                                    <Button Content="Удалить" Click="Delete_Click" Padding="8,4"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>